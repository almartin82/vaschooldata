# Standardize graduation data column names

Converts raw VDOE column names to standardized lowercase_snake_case.

## Usage

``` r
standardize_grad_columns(raw_data, era)
```

## Arguments

- raw_data:

  Raw data frame

- era:

  Data era ("v1" or "v2")

## Value

Data frame with standardized column names
