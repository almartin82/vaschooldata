# Check if directory cached data exists and is valid

Check if directory cached data exists and is valid

## Usage

``` r
dir_cache_exists(type, max_age = 7)
```

## Arguments

- type:

  Cache type ("directory_tidy" or "directory_raw")

- max_age:

  Maximum age in days (default 7 for directory data)

## Value

TRUE if valid cache exists
