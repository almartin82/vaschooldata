# Extract enrollment columns from raw data

Maps VDOE column names to standard enrollment column names.

## Usage

``` r
extract_enrollment_columns(df)
```

## Arguments

- df:

  Data frame with raw VDOE data

## Value

Data frame with standardized enrollment columns
