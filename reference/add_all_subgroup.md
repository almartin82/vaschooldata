# Add "all" subgroup to graduation data

Adds a summary record that aggregates all diploma types.

## Usage

``` r
add_all_subgroup(tidy_data)
```

## Arguments

- tidy_data:

  Tidy graduation data

## Value

Data frame with added "all" subgroup records
