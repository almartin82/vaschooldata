# Calculate state aggregates (for v1 data)

For v1 era (2019-2022), calculates state-level aggregates by summing all
school-level records.

## Usage

``` r
calc_state_aggregates(tidy_data)
```

## Arguments

- tidy_data:

  Tidy graduation data

## Value

Data frame with added state aggregate records
