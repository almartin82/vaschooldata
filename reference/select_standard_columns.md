# Select standard columns for output

Select standard columns for output

## Usage

``` r
select_standard_columns(df)
```

## Arguments

- df:

  Data frame with processed data

## Value

Data frame with only standard columns
