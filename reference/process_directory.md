# Process raw directory data

Transforms raw VDOE directory data into a standardized schema with clean
column names.

## Usage

``` r
process_directory(raw_data)
```

## Arguments

- raw_data:

  Data frame from get_raw_directory

## Value

Processed data frame with standardized columns
